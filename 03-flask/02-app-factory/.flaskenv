# This file is used to load environment variables for your 
#   application at runtime. For Flask, this requires the
#   python-dotenv package to work. Each line here is a key
#   value pair. Theses keys have special meaning for Flask
#   and define certain options/behaviors
FLASK_APP=run
FLASK_ENV=development
FLASK_DEBUG=1
